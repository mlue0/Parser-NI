# SimpleMeasure UI — React Native sample

This is a small React Native sample demonstrating the three mobile UI tabs (Parameters, Metrics, Standards) on a pure black, neon-accented theme. It's intended as a visual prototype — install React Native and run on device/emulator.

Quick start:

```bash
cd design_ui/react_native
npm install
npm run start
``` 
